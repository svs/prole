%h1 blog
- entries.keys.sort.reverse.each do |d|
  %a.l{:href => "/blog/#{CGI::escape(entries[d][:title])}"}
    .post{:style => "background: url(#{entries[d][:pic]})"}
      .p
        = entries[d][:title].split(".")[0]
